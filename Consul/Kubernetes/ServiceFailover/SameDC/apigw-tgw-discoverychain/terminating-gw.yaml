apiVersion: consul.hashicorp.com/v1alpha1
kind: TerminatingGateway
metadata:
  name: terminating-gateway # Must match the name generated by Helm
  namespace: default
spec:
  services:
    - name: static-server-nonmesh
