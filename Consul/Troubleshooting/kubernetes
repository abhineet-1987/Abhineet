# To take cluster dump of the kubernetes cluster using :-
kubectl cluster-info dump --all-namespaces --output-directory=/tmp/cluster-dump-$(date +%Y-%m-%d_%H-%M)

# kubectl get events -n consul

# kubectl logs <pod-name> -c <container-name>

# kubectl describe pod <pod-name>
