global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "nomad"
    metrics_path: /v1/metrics
    params:
      format: ['prometheus']
    static_configs:
      - targets:
          - "<nomad-server-ip>:4646"
          - "<nomad-client-ip>:4646"
